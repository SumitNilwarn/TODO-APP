# features/profile

Profile feature module.

## Presentation (Phase 9 — route reserved, screen is a placeholder)

- `presentation/profile_page.dart` — placeholder for `/profile`

## Planned contents (later phases, not implemented)

- `data/` — profile repository over the foundation `ApiClient`
  (`GET/PUT/PATCH /api/v1/profile`)
- `domain/` — profile model and editing rules
- `presentation/` — profile read/edit form