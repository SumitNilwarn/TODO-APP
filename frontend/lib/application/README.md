# `application`

Application-level state management and orchestration.

Reserved for Phase 3+: global stores, app state (auth session, navigation),
and cross-feature state coordination. No implementation exists yet by design —
Phase 1 only lays out the package so future work lands in the right place
without restructuring.

## Planned contents (not implemented)

- Auth/session store
- App-wide lifecycle/state
- Cross-feature use-case orchestration